{
  "name": "forgevia-monorepo",
  "private": true,
  "packageManager": "npm@10.8.2",
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "cms"
  ],
  "scripts": {
    "dev": "concurrently -n web,api -c green,blue \"npm --workspace apps/web run dev\" \"npm --workspace apps/api run dev\"",
    "dev:lan": "npm --workspace apps/web run dev:lan",
    "build": "npm --workspace apps/web run build && (npm --workspace apps/api run build || true)",
    "lint": "npm --workspace apps/web run lint || true",
    "test": "echo 'no tests yet'",
    "deploy:web": "npm --workspace apps/web run build && cd apps/web && npx vercel --prod --name growwithus || true"
  },
  "devDependencies": {
    "concurrently": "8.2.2"
  },
  "dependencies": {
    "lucide-react": "^0.544.0",
    "vercel": "^48.1.4"
  }
}
